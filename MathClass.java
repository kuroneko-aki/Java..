public class MathClass {

    public static void main(String[] args) {
        
        double x = 3.14;
        double y = 10;
        
        double z = Math.max(x, y);
        //double z = Math.min(x, y); min is minimun
        //double z = Math.abs(y);
        //double z = Math.sqrt(y); sqrt is the square root
        //double z = Math.round(x); rounded
        //double z = Math.ceil(x); ceil is always rouned up
        //double z = Math.floor(x); always rounded down

        System.out.println(z);
    }
    
}
